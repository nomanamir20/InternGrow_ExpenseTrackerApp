/// Centralized Hive box names — avoids typos scattered across the codebase.
class HiveBoxes {
  HiveBoxes._();

  static const settings = 'settings';
  static const transactions = 'transactions';
  static const categories = 'categories';
  static const budgets = 'budgets';
}